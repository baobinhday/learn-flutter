import 'package:flutter/material.dart';
import 'package:flutter_app/features/tv_remote/remote_bloc.dart';

class TvRemote extends StatelessWidget {
  TvRemote({super.key});

  final bloc = RemoteBloc();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}